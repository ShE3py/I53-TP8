# TEST: [0] => [0]
# TEST: [10] => [55]
# TEST: [15] => [610]
# TEST: [30] => [832040]
FONCTION fibo(i)
DÉBUT
	SI i <= 1 ALORS
		RENVOYER i;
	SINON
		RENVOYER fibo(i - 2) + fibo(i - 1);
	FSI
FIN

FONCTION main()
DÉBUT
	LIRE n;
	AFFICHER fibo(n);
FIN
