# TEST: [ 0,  0] => [1, 0, 1, 1, 0, 0]
# TEST: [ 1, -1] => [0, 1, 1, 0, 1, 0]
# TEST: [-1,  1] => [0, 1, 0, 1, 0, 1]
FONCTION main()
DÉBUT
	LIRE x;
	LIRE y;

	AFFICHER x == y;
	AFFICHER x != y;
	AFFICHER x >= y;
	AFFICHER x <= y;
	AFFICHER x > y;
	AFFICHER x < y;
FIN
